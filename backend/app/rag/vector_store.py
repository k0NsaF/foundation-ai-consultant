class VectorStore:
    def __init__(self, collection_name: str = "foundations"):
        print("⚠️ VectorStore отключён (Qdrant удалён)")
    
    def search(self, query: str, limit: int = 5) -> list:
        print("⚠️ Поиск отключён (Qdrant удалён)")
        return []